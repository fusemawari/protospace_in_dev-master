.container
  .row
    .tags-list
      %h2 Tags list
      %ul
        -@tag.each do |tag|
          %li.col-md-4
            = link_to tag.name, tag_path(tag.id),class: 'btn btn-default',style: 'width: 100%;'
